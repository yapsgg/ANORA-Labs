/* Routes into the app. Keep these in sync with the Next.js app router. */
export type Routes = { studio: string; signin: string };

export const ROUTES: Routes = { studio: "/projects", signin: "/login" };
